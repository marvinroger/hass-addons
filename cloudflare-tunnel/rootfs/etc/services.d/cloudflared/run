#!/usr/bin/with-contenv bashio

config_path=$(bashio::config "CONFIG_PATH")

bashio::log.info "Starting cloudflared..."

exec /opt/cloudflared tunnel --metrics 0.0.0.0:8099 --config "$config_path" run
